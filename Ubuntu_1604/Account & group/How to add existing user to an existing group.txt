How to add existing user to an existing group ?



Liist of Groups on ubuntu server
	
	$ cat /etc/group


To add a user to a specific group use	

	$ sudo usermod -a -G groupName userName
	
	$ sudo usermod -G name-of-group -a 'name-of-user'
	